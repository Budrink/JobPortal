import { wait } from '../GetData/wait';
import { FreelancerData } from '../Data/Data';
import { http } from '../Data/Http';

export const RepostJob = async (jobId: string): Promise<boolean> => {
  // export const getCategoryList = (): CategoryData[] => {

  //Функция для получения списка с севрера
  //   await fetch('http://localhost:17525/api/countries')
  //     .then((res) => res.json())
  //     .then((body) => {
  //       categoryList = body;
  //     })
  //     .catch((err) => {
  //       console.error(err);
  //     });
  await wait(500);

  return true;
};
export const DeleteJob = async (jobId: string): Promise<boolean> => {
  // export const getCategoryList = (): CategoryData[] => {

  //Функция для получения списка с севрера
  //   await fetch('http://localhost:17525/api/countries')
  //     .then((res) => res.json())
  //     .then((body) => {
  //       categoryList = body;
  //     })
  //     .catch((err) => {
  //       console.error(err);
  //     });
  await wait(500);

  return true;
};
