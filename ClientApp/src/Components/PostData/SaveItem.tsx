import { wait } from '../GetData/wait';
//Send the id of Freelancer wich will be add to savedItems
export const SaveFreelancer = async (
  senderId: string,
  freelancerId: string,
  saved: boolean,
): Promise<boolean> => {
  // export const getCategoryList = (): CategoryData[] => {

  //Функция для получения списка с севрера
  //   await fetch('http://localhost:17525/api/countries')
  //     .then((res) => res.json())
  //     .then((body) => {
  //       categoryList = body;
  //     })
  //     .catch((err) => {
  //       console.error(err);
  //     });
  await wait(500);

  return true;
};
//Send the id of Job wich will be add to savedItems
export const SaveJob = async (
  senderId: string,
  jobId: string,
  saved: boolean,
): Promise<boolean> => {
  // export const getCategoryList = (): CategoryData[] => {

  //Функция для получения списка с севрера
  //   await fetch('http://localhost:17525/api/countries')
  //     .then((res) => res.json())
  //     .then((body) => {
  //       categoryList = body;
  //     })
  //     .catch((err) => {
  //       console.error(err);
  //     });
  await wait(500);

  return true;
};
//Send the id of Company wich will be add to savedItems
export const SaveCompany = async (
  senderId: string,
  companyId: string,
  saved: boolean,
): Promise<boolean> => {
  // export const getCategoryList = (): CategoryData[] => {

  //Функция для получения списка с севрера
  //   await fetch('http://localhost:17525/api/countries')
  //     .then((res) => res.json())
  //     .then((body) => {
  //       categoryList = body;
  //     })
  //     .catch((err) => {
  //       console.error(err);
  //     });
  await wait(500);

  return true;
};
