import { wait } from '../GetData/wait';

export const SendOffer = async (
  senderId: string,
  freelancerId: string,
  jobId: string,
  description: string,
  deadLine: string,
) => {
  return true;
};
